import express from 'react'; // Placeholder for Express
// import cors from 'cors';
// import router from './routes';

const app: any = {}; // Mocking app object

// Middlewares
// app.use(cors());
// app.use(express.json());

// Routes
// app.use('/api', router);

export default app;
